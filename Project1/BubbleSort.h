#pragma once

#ifndef BUBBLE_SORT_H
# define BUBBLE_SORT_H

#include "Sequence.h"
#include "ArraySeq.h"
#include "ListSeq.h"
/*
template <typename T> void ArraySequence<T> :: BubbleSort()
{
	for (int i = 0; i < this->GetLenght(); i++)
		for (int j = 0; j < this->GetLenght() - 1; j++)
			if (this->Get(j) > this->Get(j + 1))
				{
					T temp = this->Get(j);
					this->InsertAt(j, this->Get(j + 1));
					this->InsertAt(j + 1, temp);
				}
}
*/
#endif
